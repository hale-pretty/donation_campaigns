scalar Upload

type Campaign {
	id: Int!
	title: String!
	description: String!
	goalAmount: Int!
	raisedAmount: Int
	startDate: String!
	endDate: String!
	status: String!
	image: String!
	user: User
}

type User {
	id: Int!
	name: String!
	email: String!
	campaigns: [Campaign]
}

type Query {
	campaigns: [Campaign]
	campaign(id: Int!): Campaign
}

type Mutation {
	createCampaign(
		title: String!
		description: String!
		goalAmount: Int!
		endDate: String!
		image: Upload!
		userId: Int!
	): Campaign

	updateCampaign(
		id: Int!
		title: String
		description: String
		goalAmount: Int
		endDate: String
		image: Upload
	): Campaign

	deleteCampaign(id: Int!): Boolean
}
