# schema.graphql

type Donation {
  id: Int!
  userId: Int!
  campaignId: Int!
  createdAt: String!
}

type Query {
  getDonationsByUser(): [Donation!]!
  getDonationsByCampaign(campaignId: Int!): [Donation!]!
}

type Mutation {
  createDonation(campaignId: Int!, amount: Int!): Donation!
}
